= simple_form_for(@cart_product) do |f|
  = f.error_notification

  .form-inputs
    = f.association :user
    = f.association :product
    = f.input :quantity

  .form-actions
    = f.button :submit
