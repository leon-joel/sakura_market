%h1 ショッピングカート

%table.table
  %thead
    %tr
      %th Image
      %th Product
      %th Price
      %th Quantity
      %th
      %th

  %tbody
    - @cart_products.each do |cart_product|
      %tr
        %td
          = image_tag( "/images/#{cart_product.product.picture}", class: :product_img )
        %td= cart_product.product.name
        %td= cart_product.product.price
        %td= cart_product.quantity
        -#%td= link_to 'Show', cart_product
        %td
          = link_to 'カートから外す', cart_product, :method => :delete,
            class: 'btn btn-info btn-xs',
            data: { confirm: '本当にカートから外してもよろしいですか？' }

%br

= link_to '商品一覧に戻る', :root
