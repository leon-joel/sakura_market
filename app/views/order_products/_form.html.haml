= simple_form_for(@order_product) do |f|
  = f.error_notification

  .form-inputs
    = f.association :order
    = f.association :product
    = f.input :sales_price
    = f.input :quantity

  .form-actions
    = f.button :submit
